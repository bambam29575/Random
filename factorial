#include <stdio.h>
int factorial(int n){
    if(n == 1){
        return 1;
    }
    int value;
    value = n*factorial(n-1);
    return value;
}


int main()
{
    int num,value;
    scanf("%d",&num);
    
    printf("The factorial of %d is %d",num,factorial(num));

    return 0;
}
