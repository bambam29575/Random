#include <stdio.h>
#include <ctype.h>


int main() {
   char ch[50];
   scanf("%s",ch);
   int count = 0;
   for(int i = 0; ch[i]!='\0'; i++){
       count++;
   }
   printf("The size of string is %d", count);
   return 0;
}
